# apps/agents

Agent runners and integrations (e.g., Eliza/agent0, external plugins).

- Run autonomous agents/strategies that interact with Babylon via `@babylon/core-*` or the API.
- Consume `apps/server` over HTTP/SSE/WS as needed; use `@babylon/shared-infra` for any direct infra access.
