# Shared (required)
# Note: all apps load env from the root .env/.env.local; avoid per-app .env files.
DATABASE_URL="postgres://user:pass@host:5432/dbname"
# Optional: Neon pooler URL (not used by app code yet; keep if you use pgbouncer/pooling)
# DATABASE_URL_POOLER="postgres://user:pass@host:5432/dbname"

# Server (optional unless you need CORS restrictions)
# CORS_ORIGIN="http://localhost:3001"

# Auth - Privy (required)
PRIVY_APP_ID="your-privy-app-id"
PRIVY_APP_SECRET="your-privy-app-secret"

# Web (client-facing) — keep NEXT_PUBLIC_* here, not in apps/web/.env
NEXT_PUBLIC_SERVER_URL="http://localhost:3000"
NEXT_PUBLIC_PRIVY_APP_ID="your-privy-app-id"

# Add new env vars here with comments/defaults and optional/required notes.
